# Demo fixtures

Tiny files for Reviewer / manual QA.

- Load these in ShipReceipt (or hash with `sha256sum`).
- Compare to `checksums.sha256.gold`.
- Flip one byte in `hello.txt` and confirm the hash changes.
